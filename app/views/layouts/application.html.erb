<!DOCTYPE html>
<html>
  <head>
    <title>EntryExit</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("exit.png", class: :exit_logo), root_path %>
            <%= link_to image_tag("logo.png", class: :logo), root_path %>
            <%= link_to image_tag("exit.png", class: :exit_logo), root_path %>
          </div>
          <%# ログインしているときは以下を表示するようにしましょう %>
          <div class="nav__right">
            <div class="menu">
              <button class="menu__button">
                メニュー
                <%= image_tag("menu.png", class: "menu__image") %>
              </button>
              <ul class="menu__list">
                <% if user_signed_in? %>
                  <li><%= link_to "物件登録", new_home_path, class: :nav__btn %></li>
                  <li><%= link_to "現場管理者登録", new_supervisor_path, class: :nav__btn %></li>
                  <li><%= link_to "建設地登録", new_address_path, class: :nav__btn %></li>
                  <li><%= link_to "職種登録", new_work_path, class: :nav__btn %></li>
                  <li><%= link_to "本人情報", user_path(current_user), class: :nav__btn %></li>
                  <li><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: :nav__btn %></li>
                <% else %>
                  <li><%= link_to "ログイン", new_user_session_path, class: :nav__btn %></li>
                  <li><%= link_to "新規登録", new_user_registration_path, class: :nav__btn %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright © ENTRYEXIT All rights reserved.</p>
    </footer>
  </body>
</html>
